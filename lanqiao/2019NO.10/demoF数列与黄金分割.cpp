#include <bits/stdc++.h>

using namespace std;


int main()
{
    vector<int> dp(2000000001, 0);
    // dp[0] = 1;
    // dp[1] = 1;
    // for (int i = 2; i <= N; i++)
    //     dp[i] =  dp[i-1] + dp[i-2];

    // for(int i = 1; i < 100; i++)
    //     cout << dp[i] << " ";
          
    return 0;
}